import Phaser from 'phaser';
import { EMPTY,GREY,ORANGE,cloneGrid,type CellValue,type Grid,type Level,type Position } from '../core/model';
import { includesPosition,validateGrid } from '../core/validator';
import { AudioService } from '../services/AudioService';
import { HapticsService } from '../services/HapticsService';
interface CellView {container:Phaser.GameObjects.Container;bg:Phaser.GameObjects.Graphics;cat:Phaser.GameObjects.Image|undefined;preset:boolean;r:number;c:number}
export class BoardView extends Phaser.GameObjects.Container {
  readonly grid:Grid;private cells:CellView[]=[];private readonly inset:number;private readonly tile:number;errors=0;onChanged?:()=>void;
  constructor(scene:Phaser.Scene,x:number,y:number,readonly level:Level,private readonly boardSize:number){super(scene,x,y);scene.add.existing(this);this.grid=cloneGrid(level.initial);this.inset=boardSize*.115;this.tile=(boardSize-this.inset*2)/level.size;const shadow=scene.add.ellipse(10,boardSize*.48,boardSize*.72,70,0x5c3d49,.18);const frame=scene.add.image(0,0,'board-frame').setDisplaySize(boardSize,boardSize);this.add([shadow,frame]);this.buildCells();this.buildConstraints();}
  private buildCells(){const start=-this.boardSize/2+this.inset;for(let r=0;r<this.level.size;r++)for(let c=0;c<this.level.size;c++){const holder=this.scene.add.container(start+c*this.tile+this.tile/2,start+r*this.tile+this.tile/2);const bg=this.scene.add.graphics();holder.add(bg);const preset=this.level.initial[r]![c]!==EMPTY;const cell:CellView={container:holder,bg,cat:undefined,preset,r,c};this.cells.push(cell);this.add(holder);this.paintCell(cell,false);if(this.grid[r]![c]!==EMPTY)this.setCat(cell,this.grid[r]![c]!,false);if(!preset)holder.setSize(this.tile*.9,this.tile*.9).setInteractive({useHandCursor:true}).on('pointerdown',()=>this.cycle(cell));}}
  private paintCell(cell:CellView,error:boolean,hint=false){const value=this.grid[cell.r]![cell.c]!,s=this.tile*.87;const fill=error?0xffe4df:hint?0xfff0bd:value===GREY?0xeaf2f5:value===ORANGE?0xffede1:0xfff8ed;const stroke=error?0xeb7a7d:value===GREY?0x9fbac3:value===ORANGE?0xe9ad85:0xe4b98e;cell.bg.clear().fillStyle(0x714650,.10).fillRoundedRect(-s/2+3,-s/2+5,s,s,this.tile*.14).fillStyle(fill).lineStyle(Math.max(2,this.tile*.025),stroke,.78).fillRoundedRect(-s/2,-s/2,s,s,this.tile*.14).strokeRoundedRect(-s/2,-s/2,s,s,this.tile*.14);if(cell.preset)cell.bg.fillStyle(0xffffff,.3).fillCircle(-s*.31,-s*.31,this.tile*.035);}
  private setCat(cell:CellView,value:CellValue,animate=true){cell.cat?.destroy();cell.cat=undefined;if(value===EMPTY)return;const small=this.level.size>=6,texture=value===GREY?(small?'grey-small':'grey-cat'):(small?'orange-small':'orange-cat');const image=this.scene.add.image(0,0,texture);const max=this.tile*(this.level.size===4?.62:this.level.size===6?.58:.54);image.setScale(Math.min(max/image.width,max/image.height));if(cell.preset)image.setAlpha(.9);cell.container.add(image);cell.cat=image;if(animate){const sx=image.scaleX,sy=image.scaleY;image.setScale(sx*.8,sy*.8).setAlpha(.35).setAngle(value===GREY?-2:2);this.scene.tweens.add({targets:image,scaleX:sx*1.05,scaleY:sy*1.05,alpha:1,duration:135,ease:'Back.Out',onComplete:()=>this.scene.tweens.add({targets:image,scaleX:sx,scaleY:sy,angle:0,duration:85})});}}
  private cycle(cell:CellView){const current=this.grid[cell.r]![cell.c]!,next:CellValue=current===EMPTY?GREY:current===GREY?ORANGE:EMPTY;this.grid[cell.r]![cell.c]=next;this.paintCell(cell,false);this.setCat(cell,next);AudioService.play('place');void HapticsService.light();const result=validateGrid(this.grid,this.level.constraints);if(result.errors.length){this.errors++;AudioService.play('invalid');void HapticsService.error();this.scene.tweens.add({targets:cell.container,x:`+=${this.tile*.035}`,duration:42,yoyo:true,repeat:3});}this.paintErrors(result.errors);this.onChanged?.();}
  private paintErrors(errors:Position[]){for(const cell of this.cells)this.paintCell(cell,includesPosition(errors,cell.r,cell.c));}
  private buildConstraints(){const start=-this.boardSize/2+this.inset;for(const constraint of this.level.constraints){const [ar,ac]=constraint.a,[br,bc]=constraint.b,x=start+((ac+bc)/2+.5)*this.tile,y=start+((ar+br)/2+.5)*this.tile;const image=this.scene.add.image(x,y,constraint.type==='same'?'same':'different'),scale=Math.min(this.tile*.25/image.width,this.tile*.25/image.height);image.setScale(scale).setDepth(20);this.add(image);}}
  reset(){for(const cell of this.cells){this.grid[cell.r]![cell.c]=this.level.initial[cell.r]![cell.c]!;this.paintCell(cell,false);this.setCat(cell,this.grid[cell.r]![cell.c]!,false);}this.errors=0;this.onChanged?.();}
  reveal(position:Position,value:CellValue){const cell=this.cells.find(x=>x.r===position[0]&&x.c===position[1]);if(!cell||cell.preset)return;this.grid[cell.r]![cell.c]=value;this.paintCell(cell,false,true);this.setCat(cell,value);this.scene.tweens.add({targets:cell.container,scale:1.12,duration:150,yoyo:true});this.onChanged?.();}
}
