import type { CapacitorConfig } from '@capacitor/cli';
const config: CapacitorConfig = { appId: 're.meowza.game', appName: 'Meowza', webDir: 'dist', backgroundColor: '#fff6ec', android: { allowMixedContent: false } };
export default config;
